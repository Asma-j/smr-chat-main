export interface MsItem {
    name: string;
    resume?: string;
    link?: string;
    startDate?: Date;
    endDate?: Date;
    location?: string;
    logoUrl?: string;
    // type: ProjectType; // enum
    // technologies: Technology[];
}